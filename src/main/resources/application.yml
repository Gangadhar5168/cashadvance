spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cash_advance
    username: root
    password: Aug12025
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
  application:
    name: cash-advance
server:
  port: 8080

security:
  remember-me:
    key: default-remember-me-key
