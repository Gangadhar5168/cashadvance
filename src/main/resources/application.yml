spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cash_advance
    username: root
    password: Aug12025
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  application:
    name: cash-advance
server:
  port: 8080
